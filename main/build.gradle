apply plugin: 'com.android.library'

android {
    compileSdkVersion 29
    buildToolsVersion "29.0.3"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 29
        versionName "2020.6.17.1546"
    }
}

dependencies {
    testImplementation 'junit:junit:4.12'
}