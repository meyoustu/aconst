apply plugin: 'com.android.library'

android {
    compileSdkVersion 30
    buildToolsVersion "30.0.0"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 30
        versionName "2020.7.1.2137"
    }
}

dependencies {
    testImplementation 'junit:junit:4.12'
}